[package]
name = "plugin-greeter"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
plugin-system = { path = "../plugin-system" }
plugin-types = { path = "../plugin-types" }
