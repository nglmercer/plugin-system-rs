[package]
name = "plugin-key-simulator"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
plugin-system = { path = "../../crates/plugin-system" }
plugin-interfaces = { path = "../../crates/plugin-interfaces" }
log = "0.4"
rdev = "0.5"
