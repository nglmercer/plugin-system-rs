# Build outputs
/target/
/dist/
/releases/
# Test fixture build outputs (separate from main /target/)
/crates/plugin-system/tests/cabi-fixture/target/
web/dist/
web/package-lock.json

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local

# Node
node_modules/

# Logs
*.log
npm-debug.log*

# Coverage
coverage/
*.gcda
*.gcno
data/dashboard.json
macos-sdk/
*.dll
*.so
*.dylib
data/plugin-state.json

# wasm plugin crates are outside the workspace and build into their own target/
plugins/*-wasm/target/
# Staged build output: `sd-plugins build` copies each component and its
# manifest here. The sources live in plugins/<crate>/.
plugins/*.wasm
plugins/*.manifest.json
